<h1>投稿一覧</h1>

<% @posts.each do |post| %>
<div>
  <%= image_tag post.get_image %>
  <p><%= image_tag "default-image.jpg"%><%= post.user.name %></p>
  <p><%= post.created_at.strftime('%Y/%m/%d') %></p>
</div>
<% end %>
